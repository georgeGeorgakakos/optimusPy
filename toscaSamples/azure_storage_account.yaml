tosca_definitions_version: tosca_simple_yaml_1_3
description: Azure Storage Account

topology_template:
  node_templates:
    storage_account:
      type: tosca.nodes.Storage.Azure.StorageAccount
      properties:
        account_name: mystorageaccount
        account_kind: StorageV2
        sku: Standard_LRS
        access_tier: Hot
        enable_https_only: true
