tosca_definitions_version: tosca_simple_yaml_1_3
description: Kubernetes Secret for credentials

topology_template:
  node_templates:
    db_credentials:
      type: tosca.nodes.Kubernetes.Secret
      properties:
        name: db-credentials
        type: Opaque
        data:
          username: YWRtaW4=
          password: cGFzc3dvcmQxMjM=
      requirements:
        - host: k8s_cluster
    
    k8s_cluster:
      type: tosca.nodes.Kubernetes.Cluster
      properties:
        version: "1.28"
